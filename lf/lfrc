set hidden true
set ignorecase true
#set icons true

# open nvim with lf
cmd open $nvim.exe %f%
map e $nvim.exe %f%
map w $pwsh %f%

# go to partition
map ga cd A:\\

# commands of copy, paste, cut and delete
map c copy %f%
map p paste %f%
map x cut %f%
map d delete %f%
